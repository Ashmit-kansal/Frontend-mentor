
function Header() {
  return (
    // {/* header will remain fixed at the top of the page */}
        <header>
          {/* this div will show which subject is currently selected */}
          <div></div>
          {/* this div is for theme switcher button */}
          <div></div>
        </header>
  )
}

export default Header